// import championJson from "../datas/champion.json";

// const supportLaner = championJson.filter((champion) => {
//   return champion ? champion.tags.includes("Support") : null;
// });

// function championByRole(role) {
//   console.log(role);
//   championJson.filter((champion) => {
//     return champion ? champion.tags.includes(role) : null;
//   });
// }

// export default championByRole;
